const exrpess = require("express")
const dbconnet = require("../config/database")
const userModel = require("../models/user.model")

exports.shingup  = async  (res , req) => {
     dbconnet()

     try {
        
     } catch (error) {
        
     }
}